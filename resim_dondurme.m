
i=imread("c:/octave/resimler/house.tiff");

 [sat,sut,r] = size(i);
 
 h=uint8(zeros(sat,sut,r));
 
 r
 
 %x0=round(sat/2);
 %y0=round(sut/2);
 
 
 %teta = pi/13;
 
 
 
 
%for x1=1:sat
% for y1=1:sut
% i(x1,y1,4)=50;
 
 %   x2=round(cos(teta)*(x1-x0)-sin(teta)*(y1-y0)-x0);
 %   y2=round(sin(teta)*(x1-x0)+cos(teta)*(y1-y0)+y0);
%    if (x2>=1)&&(x2<=sat)&&(y2>=1)&&(y2<=sut)
%      h(x1,y1,:)=i(x2,y2,:);
%    end
 %end
 %end

 %imshow(i);
 
 
 